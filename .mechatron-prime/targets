# Mechatron Prime CI targets for pmarreck/random.
# One flake attribute per line; the worker fetches this file from the exact
# pushed SHA.
#
# checks.x86_64-linux.random-test runs `FAST=1 bash ./test`, which executes all
# suites except kernel_jit_diff (deep-mode only by design -- it is 30s of a 42s
# run, and the mitigation it guards is already covered deterministically in the
# default run by fixed_test's jit.attach check).
checks.x86_64-linux.random-test
checks.x86_64-linux.stats-smoke
checks.x86_64-linux.package-smoke
checks.x86_64-linux.random-crossarch
checks.x86_64-linux.windows-x64-smoke

# The combined package: proves the Lua CLI, C CLI, static Zig library and public
# header install together.
packages.x86_64-linux.default

# Explicit C-CLI package surface (same combined output, randomz app entrypoint).
packages.x86_64-linux.randomz
